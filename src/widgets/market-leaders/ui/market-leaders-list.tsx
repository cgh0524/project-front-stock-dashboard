import type { Option } from "@/shared/lib/types";

export type MarketLeadersListProps = {
  selectedTab: Option;
};

export const MarketLeadersList = ({ selectedTab }: MarketLeadersListProps) => {
  return <div>MarketLeadersList</div>;
};
